// theme.ts
import { extendTheme } from '@mui/material/styles';

export const theme = extendTheme({
    colorSchemes: {
        light: { palette: {} },   // customize if you want
        dark: { palette: {} },
    },
});